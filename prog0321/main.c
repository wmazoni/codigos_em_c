#include <stdio.h>

int main() {
    /* Versão 1 */
    int n;
    printf("Introd. um Nº: ");scanf("%d",&n);
    if (n==0)
        printf("Número é igual a zero\n");
    else
        printf("Número não é igual a zero\n");
}